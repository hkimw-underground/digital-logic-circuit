# 세부 개발 프롬프트 03: Python 백엔드 및 비전 AI 최적화 (담당자: 김현우)

## 1. 단계 목적
이 단계에서는 Python 서버의 성능을 극한으로 끌어올리고, 비전 AI의 정확도와 처리 속도를 최적화합니다. 대규모 로그 발생 시의 DB 성능 저하를 방지하고, 영상 분석 지연을 최소화합니다.

## 2. To-Do 리스트
1. **데이터베이스 접근 최적화:** SQLite의 WAL(Write-Ahead Logging) 모드를 활성화하고, 쿼리 인덱싱을 통해 대용량 로그 검색 속도를 개선합니다.
2. **비전 AI 병목 현상 제거:** 얼굴 검출과 인식 과정을 분리하거나, 프레임 스킵 기술을 도입하여 초당 프레임 수(FPS)를 확보합니다.
3. **비동기 처리 도입:** FastAPI의 엔드포인트와 내부 프로세스를 검토하여, 블로킹 함수가 메인 이벤트 루프를 방해하지 않도록 스레드 풀 또는 비동기 처리를 강화합니다.
4. **보안 강화:** bcrypt 해싱 강도를 조절하고, 시리얼 통신 상의 데이터 유효성 검증 로직을 추가합니다.

## 3. 개발 가이드 (Vibe)
- 코드의 성능을 측정할 수 있는 프로파일링 코드를 포함하여, 최적화 전후의 성능 수치를 제시하십시오.
- `vision_ai.py`에서 불필요한 모델 로드 과정을 줄이고 캐싱을 활용합니다.

## 4. AI 명령 예시
"`server/vision_ai.py`의 `verify_face` 함수가 실행되는 동안 웹 GUI의 실시간 영상 피드가 끊기는 현상이 있어. 이를 해결하기 위해 영상 분석 로직을 별도의 스레드에서 실행하거나, 분석 주기를 최적화하는 코드를 작성해줘. 또한 SQLite DB 연결 시 `check_same_thread=False` 옵션을 적절히 활용하고 성능을 높일 수 있는 설정값들을 제안해줘."
