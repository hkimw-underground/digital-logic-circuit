# 📊 2FA 스마트 도어락 저장소 리뷰 (Repository Evaluation)

## 1. 아키텍처 및 시스템 설계 (Architecture & Design) 🟢 우수
* **역할 분리:** 하드웨어 제어(`main.py`, Arduino), 데이터베이스 관리(`database.py`), 비전 AI(`vision_ai.py`), 웹 서버(`web_app.py`) 등 모듈별 책임이 명확하게 분리되어 있습니다.
* **이중 인증(2FA) 논리:** 1차 인증(NFC/PIN) 후 2차 인증(YOLO 객체 인식 + 얼굴 매칭)으로 넘어가는 흐름이 매우 견고합니다. 특히, `VisionAI` 클래스에서 스푸핑 방지를 위해 눈 깜빡임(Blink) 및 전자기기(Phone/Screen)를 감지하는 로직이 훌륭합니다.
* **비동기 알림:** Discord 웹훅 발송을 `threading.Thread`로 분리하여 서버의 메인 이벤트 루프(인증/릴레이 제어)를 블로킹하지 않도록 설계된 점이 실무적입니다.

## 2. 보안 분석 (Security Analysis) 🟡 양호 (일부 개선 필요)
* **비밀번호 관리:** 평문 비밀번호 저장을 제거하고 `bcrypt` 단방향 해시를 도입한 점은 필수적이고 훌륭한 조치입니다. 타이밍 공격(Timing Attack) 방지를 위해 인증 시루프를 도는 설계도 인상적입니다.
* **스푸핑 방지:** 단순 얼굴 매칭에 그치지 않고, 화면에 띄운 사진이나 영상을 구별해 내는 정책(`DOORLOCK_YOLO_PHONE_CLASSES`)은 높은 보안 수준을 보여줍니다.
* **보안 한계점 (직렬 통신):** 문서(`SECURITY_ANALYSIS.md`)에도 명시되어 있듯이, 서버와 Arduino 간의 시리얼 통신(`OPEN_DOOR` 등)이 평문으로 전송되고 있습니다. 물리적 접근 시 패킷 스니핑/주입 공격이 가능하므로, 챌린지-리스폰스(Challenge-Response) 구조 도입이 필요해 보입니다.

## 3. 코드 품질 (Code Quality) 🟢 우수
* **코드 가독성:** 변수명과 메서드명이 직관적이며 문서화가 잘 되어 있습니다.
* **예외 처리:** 데이터베이스 연결 끊김, 파일 I/O 오류, 의존성 모듈 누락(Mock)에 대비한 방어적 코딩이 꼼꼼하게 짜여 있습니다.
* **DB 동시성 처리:** SQLite의 WAL(Write-Ahead Logging) 모드를 사용하고, 쓰기 전용 잠금(`threading.Lock`)을 적용해 웹 서버(FastAPI)와 메인 데몬 간의 충돌 가능성을 잘 통제했습니다.

## 4. 테스트 및 품질 보증 (Testing & QA) 🟢 우수
* **테스트 커버리지:** 50개의 단위 테스트가 데이터베이스 로직, 비전 모델의 바이트 인코딩, 보안 필터링 등 시스템 전반을 포괄합니다.
* **의존성 격리:** 외부 환경(`pyserial`, `cv2`, `numpy`, `bcrypt` 등)이 설치되지 않은 제한된 환경에서도 실행될 수 있도록 `mock_deps.py` 또는 내장 Mocking 시스템을 활용할 수 있게 구조화한 점이 매우 좋습니다.

## 5. 총평 및 개선 제안 💡
전체적으로 캡스톤 디자인 및 실무 프로토타입 수준 이상의 완성도를 보여줍니다. 특히, **보안에 대한 깊은 고민(스푸핑 방지, 타이밍 공격 방어, 락다운 모드)**이 코드 곳곳에 배어 있는 점이 가장 큰 장점입니다.

**[권장 개선 사항]**
1. **시리얼 통신 암호화:** Arduino와 Python 서버 간 단순 평문(WAKEUP, OPEN_DOOR) 통신을 암호화하거나 HMAC 등을 적용하여 물리적 릴레이 해킹을 방지하세요.
2. **센서 확장:** 도어가 닫혔는지를 확인하는 리드 스위치(Reed Switch) 로직이 추가되면 완벽한 출입 통제(문 열림 유지 경고 등)가 가능해집니다.
3. **의존성 관리:** `requirements.txt`에 명시된 외부 라이브러리(numpy, opencv 등)가 없는 환경을 위해 지금처럼 Mock을 활용하는 것도 좋지만, 필요하다면 Dockerfile을 추가하여 배포 환경을 완전히 표준화하는 것을 추천합니다.
